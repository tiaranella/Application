info:
  name: Event1
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/events/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
